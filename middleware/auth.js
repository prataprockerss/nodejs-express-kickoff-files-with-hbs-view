const
  session = require('express-session')

module.exports = {
  isLogged: (req,res,next) =>{
    // Auth logic for is logged 
  },
  isLogin: (req,res,next) => {
    // Auth logic for is Login 
  }
 }